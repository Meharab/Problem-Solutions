#include<bits/stdc++.h>
using namespace std;
main()
{
    int r1,c1,t,c=1,r;
    //(r1 − r2)**2 + (c1 − c2)**2 = 5
    cin>>t;
    while(c<=t)
    {
        cin>>r1>>c1;
        r=0;
        if((((r1+1) <= 8) && (r1+1) >= 1) && (((c1-2) <= 8) && (c1-2) >= 1))
            r++;
        if((r1-1) <= 8 && (c1-2) <= 8 && (r1-1) >= 1 && (c1-2) >= 1)
            r++;
        if((r1+1) <= 8 && (c1+2) <= 8 && (r1+1) >= 1 && (c1+2) >= 1)
            r++;
        if((r1-1) <= 8 && (c1+2) <= 8 && (r1-1) >= 1 && (c1+2) >= 1)
            r++;
        if((r1+2) <= 8 && (c1-1) <= 8 && (r1+2) >= 1 && (c1-1) >= 1)
            r++;
        if((r1+2) <= 8 && (c1+1) <= 8 && (r1+2) >= 1 && (c1+1) >= 1)
            r++;
        if((r1-2) <= 8 && (c1-1) <= 8 && (r1-2) >= 1 && (c1-1) >= 1)
            r++;
        if((r1-2) <= 8 && (c1+1) <= 8 && (r1-2) >= 1 && (c1+1) >= 1)
            r++;
        printf("Case %d: %d\n",c,r);
        c++;
    }
}
